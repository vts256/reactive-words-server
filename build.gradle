group 'reactive-words-server'
version '1.0.0'

apply plugin: 'java'

sourceCompatibility = 9.0

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.0.1'
}
