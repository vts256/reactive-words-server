CREATE TABLE Word(
user text,
category uuid,
word text,
translation set<text>,
answers int,
image frozen<Link>,
speech frozen<Link>,
PRIMARY KEY((user, category), word));

CREATE TYPE Link(
key text,
url text);

CREATE TABLE Category(
user text,
title text,
image frozen<Link>,
id uuid,
PRIMARY KEY(user, title));