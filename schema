CREATE TABLE Word(
user text,
category uuid,
word text,
translation set<text>,
answers int,
PRIMARY KEY((user, category), word));

CREATE TABLE Category(
user text,
title text,
id uuid,
PRIMARY KEY(user, title));