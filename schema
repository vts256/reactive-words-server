CREATE TABLE Word(
user text,
category text,
word text,
translation set<text>,
answers int,
PRIMARY KEY((user, category), word));

CREATE TABLE Category(
user text,
title text,
id timeuuid,
PRIMARY KEY(user, title));